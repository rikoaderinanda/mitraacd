@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">An error occurred while processing your request.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<div class="alert alert-warning border-0 shadow-sm p-4 rounded-3">
    <div class="d-flex align-items-start">
        <i class="bi bi-exclamation-triangle-fill text-warning me-3 fs-2"></i>
        <div>
            <h4 class="mb-2 fw-bold text-warning">Development Mode Aktif</h4>
            <p class="mb-1">
                Saat ini aplikasi berjalan dalam mode <strong>Development</strong>. Mode ini akan menampilkan informasi error yang lebih detail.
            </p>
            <p class="mb-1">
                <strong class="text-danger">Peringatan:</strong> Jangan aktifkan mode <strong>Development</strong> di lingkungan produksi karena dapat memperlihatkan informasi sensitif kepada pengguna akhir.
            </p>
            <p class="mb-0">
                Untuk debugging lokal, setel variabel <code>ASPNETCORE_ENVIRONMENT</code> ke <code>Development</code> lalu restart aplikasi.
            </p>
        </div>
    </div>
</div>
